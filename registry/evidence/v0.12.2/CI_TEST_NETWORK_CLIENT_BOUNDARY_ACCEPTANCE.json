{
  "schema": "provoware.release-ci-test-network-client-boundary.v1",
  "status": "BLOCKED",
  "scope": "quality-executed-test-source-direct-network-capability-expansion-boundary",
  "feature_freeze": true,
  "product_feature_added": false,
  "workflow": ".github/workflows/quality.yml",
  "regression_test": "tests/release_gate/test_ci_test_network_client_boundary.py",
  "contract": "Quality CI must not silently expand direct network capability in test sources actually executable in the canonical Quality workflow checkout.",
  "reviewed_network_baseline_files": 16,
  "baseline_groups": ["tests/security", "tests/startup"],
  "baseline_identity": "exact Git blob SHA per reviewed Quality-executed loopback source file",
  "source_discovery": "test-source paths are derived from .github/workflows/quality.yml; only references resolving to existing files in the current checkout are scanned, because quality.yml intentionally contains version-tolerant optional file references guarded by existence checks",
  "latest_quality_run": 65,
  "latest_quality_head": "b1f9bd3ea19b47995dab38836eaffa1dff712f08",
  "latest_result": "BLOCKED pending CI rerun after correcting source discovery for absent version-tolerant workflow references",
  "acceptance_blocker": "The concrete false positive was identified: quality.yml contains optional version-tolerant references that are intentionally absent in this checkout, while the first guard treated every textual reference as mandatory and failed before candidate scanning. Commit 9d504993ab0b12179e072448e38616ce9f03db98 now scans only references resolving to executable files, keeps the 16-file reviewed loopback baseline fail-closed, and reports absent optional references as diagnostics. Acceptance remains BLOCKED until the real Quality workflow proves the repaired guard and subsequent gates.",
  "outside_baseline_blocked": [
    "socket import or direct socket calls",
    "http.client import or direct calls",
    "urllib.request import or direct calls",
    "requests",
    "httpx",
    "aiohttp",
    "ftplib",
    "smtplib",
    "telnetlib",
    "xmlrpc.client",
    "Node http/https/net/tls/dns/dgram modules",
    "fetch",
    "WebSocket",
    "XMLHttpRequest"
  ],
  "fail_closed": [
    "Quality workflow unreadable or contains no test references",
    "Quality workflow resolves no existing test sources",
    "reviewed baseline no longer Quality-executed",
    "reviewed baseline Git blob SHA drift",
    "unparseable Python Quality test source",
    "unreadable JavaScript Quality test source",
    "direct network-capable import or call outside reviewed baseline",
    "empty non-baseline Quality source set"
  ],
  "diagnostics": [
    "absent version-tolerant workflow references are reported but not classified as executable sources",
    "direct network findings include repository-relative file and source line"
  ],
  "limitations": "Source-level expansion guard only. Existing reviewed Quality security/startup tests intentionally use local loopback networking. This does not prove an OS-level egress sandbox and does not fully exclude indirect networking through imported application modules.",
  "boundary": "No product/UI/schema/domain behavior, no sudo, no new workflow permission, no secret, no OIDC, no dependency installation and no browser/device gate promotion."
}
