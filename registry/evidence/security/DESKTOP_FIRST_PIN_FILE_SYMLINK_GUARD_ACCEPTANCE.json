{
  "evidence_id": "DESKTOP_FIRST_PIN_FILE_SYMLINK_GUARD_ACCEPTANCE",
  "status": "SOURCE_AND_RUNTIME_CONTRACT",
  "scope": "desktop-first-pin-file-path-parent-retry-and-cleanup-parent-guard",
  "release_gate_promoted": false,
  "product_feature_added": false,
  "production_entrypoint": "app/secure_response_server.py",
  "security_implementation": "app/secure_server.py",
  "test": "tests/security/test_first_pin_file_symlink_guard.py",
  "ci_step": "Desktop first-PIN file symlink guard",
  "contracts": [
    "first-start PIN parent path must be a real directory rather than a symlink",
    "dir_fd-capable platforms create the PIN relative to an opened parent directory descriptor",
    "parent symlink cannot redirect PIN creation outside the project",
    "predictable first-start PIN file path is created exclusively rather than truncating an existing entry",
    "O_NOFOLLOW is used when available to reject a symlink in the final path component",
    "symlink target content remains unchanged when bootstrap PIN-file creation is blocked",
    "a persisted active profile still accepting bootstrap PIN 0000 causes secure startup to fail closed",
    "normal first start creates a regular mode-0600 PIN file with a non-default PIN",
    "normal first start rotates the persisted profile away from bootstrap PIN 0000",
    "successful PIN verification does not authorize while one-time PIN cleanup cannot be proven",
    "cleanup failure returns 503 FIRST_PIN_CLEANUP_FAILED rather than silently authenticating",
    "authorization is cached only after successful one-time PIN cleanup",
    "cleanup rejects a substituted parent symlink rather than resolving deletion through it",
    "a substituted cleanup parent cannot cause deletion of an external same-named sentinel file",
    "failed parent cleanup preserves the real one-time credential for a safe retry",
    "dir_fd-capable platforms unlink and prove absence relative to one opened parent descriptor",
    "retry after restoring the trusted parent cleans only the real PIN file and can authenticate"
  ],
  "expected_summary": "SUMMARY total=14 passed=14 failed=0",
  "known_limits": [
    "HTTP Basic Auth remains in use",
    "no explicit invalidatable desktop logout/lock session exists yet",
    "platforms without descriptor-relative unlink/stat use explicit parent symlink/type checks with a residual TOCTOU window",
    "real browser, microphone, Android-device and iPhone-device gates remain open",
    "backup/recovery, disk-full and device-loss failure matrices remain open"
  ]
}
