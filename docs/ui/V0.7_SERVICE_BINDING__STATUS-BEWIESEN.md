# V0.7 SERVICE-BINDING — STATUS: BEWIESEN

## Laufweg
`Browser UI → localhost HTTP API → Domain Services → Mutation Queue → CoreStore/SQLite`

Die HTML-Datei wird nicht mehr als reine `file://`-App betrachtet. Der sichere Start erfolgt über `STARTEN_LINUX.sh`, wodurch UI und lokaler Service gemeinsam gestartet werden.

## Angebunden
- Memo Create + Papierkorb
- Todo Create + Complete
- Kalender Event Create
- Header nächste 10 echte Todos/Termine
- Quick-Note Append in feste TXT
- Standardeditor-/Öffnen-Adapter (Linux `xdg-open`)
- Share-Adapter (Linux `xdg-email`, Versand erst im Mailclient)
- Reminder-Adapter (`notify-send` als Linux-Referenz)
- Undo/Redo
- Settings-Grundfluss
